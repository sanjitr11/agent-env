{
  "name": "agent-env",
  "version": "0.1.0",
  "description": "Persistent specialized agent environment for solo founders — built on Claude Code",
  "type": "module",
  "bin": {
    "agent-env": "./bin/agent-env.js"
  },
  "scripts": {
    "dev": "tsx watch src/cli/init.ts",
    "build": "tsc",
    "start": "node bin/agent-env.js",
    "init": "tsx bin/agent-env.ts init",
    "route": "tsx bin/agent-env.ts route"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "zod": "^3.22.4",
    "picocolors": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
