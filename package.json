{
  "name": "agent-env-workspace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w packages/shared",
    "build:cli": "npm run build -w packages/cli",
    "build": "npm run build:shared && npm run build:cli",
    "dev:shared": "npm run dev -w packages/shared",
    "dev:cli": "npm run build:shared && npm run dev -w packages/cli",
    "dev:app": "npm run dev -w packages/app",
    "dev": "npm run build:shared && npm run dev -w packages/app"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
